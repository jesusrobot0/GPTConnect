import React from "react";
import ReactDOM from "react-dom/client";
import { GPTConnect } from "./GPTConnect";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <GPTConnect />
  </React.StrictMode>
);
